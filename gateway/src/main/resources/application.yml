server:
  port: 2222
eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

zuul:
  prefix: /api
  routes:
    ###按照顺序加载
    order: /order/**
#    order: /order/**
    store: /store/**
  sensitive-headers: Cookie,Set-Cookie